import styled from 'styled-components';
import { margin } from '@styles/typescale';

const Container = styled.div`
    max-width: 35rem;
    margin: auto;
    ${margin('bottom', 1.5)}
`;

export { Container };
